# Config

## Gateway
See `configs/gateway.yaml` for default policies and routes.

## Riskd
See `configs/riskd.yaml` for thresholds and Redis configuration.

## Sociald
See `configs/sociald.yaml` for DB/Redis and JWT secrets.
